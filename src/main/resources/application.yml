spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: ${DRIVER_NAME}
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
